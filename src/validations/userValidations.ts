import Fjs from 'fluent-json-schema'

const getByIdV = {
    tags: ['User'],
}


const updateByIdV = {
    tags: ['User'],
}

const deleteByIdV = {
    tags: ['User'],
}

export default {
    getByIdV,
}